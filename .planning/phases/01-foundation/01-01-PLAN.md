---
phase: 01-foundation
plan: 01
type: execute
domain: rust
---

<objective>
Create strict Rust workspace and deterministic candidate decision core.

Purpose: Establish compile-time quality policy and domain vocabulary every later crate depends on.
Output: Root workspace, core crate, typed metrics/gates/decisions, selection tests, and CI quality gate.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within task scope, run each `<verify>` command, and stop at blocking checkpoints. After all tasks pass, write SUMMARY.md using Outcomes, Files changed, Verification, and Follow-ups sections.
</execution_context>

<context>
Read these project files before editing:
- .planning/BRIEF.md
- .planning/ROADMAP.md
- docs/plans/2026-09-11-autoresearch-rust-platform-design.md
- .gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish strict Rust workspace</name>
  <files>Cargo.toml, rust-toolchain.toml, crates/autoresearch-core/Cargo.toml, crates/autoresearch-core/src/lib.rs, .github/workflows/ci.yml, .gitignore</files>
  <action>Create edition-2024 workspace with resolver 3, pinned stable toolchain components, shared metadata/dependencies, Clippy pedantic policy, denied unsafe code and broken rustdoc links. Add core library member and CI commands matching local gates. Keep upstream Python files in place until migration phase to avoid mixing history-preservation work into foundation.</action>
  <verify>cargo metadata --no-deps and cargo fmt --all --check pass</verify>
  <done>Workspace resolves, every crate inherits workspace lints, CI runs format, Clippy, tests, and docs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed experiment decision model</name>
  <files>crates/autoresearch-core/src/lib.rs, crates/autoresearch-core/src/metric.rs, crates/autoresearch-core/src/decision.rs</files>
  <action>Model metric kinds, directions, finite numeric values, gate outcomes, evaluation snapshots, candidate complexity, and explicit keep/discard reasons. Implement lexicographic selection: failed hard gate discards; primary regression discards; primary improvement keeps; equality compares diff lines, dependency delta, then runtime. Keep market evidence and diagnostics outside selection. Reject missing, duplicate, non-finite, or mismatched objective metrics. Avoid floating-point epsilon guessing because comparability belongs to frozen evaluator configuration.</action>
  <verify>cargo test -p autoresearch-core</verify>
  <done>Unit tests cover hard gates, improvement, regression, ties, diagnostics, market evidence, malformed metrics, and deterministic ordering.</done>
</task>

</tasks>

<verification>
- [ ] `cargo fmt --all --check`
- [ ] `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- [ ] `cargo test --workspace --all-features`
- [ ] `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps`
</verification>

<success_criteria>
- Workspace quality gates pass without warnings.
- Core API makes invalid finite metric values unrepresentable through public constructors.
- Candidate selection cannot use diagnostic or market-evidence metrics.
- Decision tests document every approved lexicographic branch.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, and Next Step sections.
</output>
