---
phase: 01-foundation
plan: 04
type: execute
domain: rust
---

<objective>
Expose safe foundation operations through stable `autoresearch` CLI.

Purpose: Let repositories create contracts, diagnose prerequisites, and freeze run identity without beginning mutation or claiming evaluator results.
Output: CLI crate with init, doctor, baseline shell, machine-readable output, exit codes, and integration tests.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within task scope, run each `<verify>` command, and write SUMMARY.md after all gates pass.
</execution_context>

<context>
Read these project files before editing:
- .planning/BRIEF.md
- .planning/ROADMAP.md
- .planning/phases/01-foundation/01-03-SUMMARY.md
- docs/plans/2026-09-11-autoresearch-rust-platform-design.md
- crates/autoresearch-config/src/lib.rs
- crates/autoresearch-core/src/journal.rs
- .gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add non-overwriting init and diagnostic commands</name>
  <files>Cargo.toml, apps/autoresearch-cli/Cargo.toml, apps/autoresearch-cli/src/main.rs, apps/autoresearch-cli/src/lib.rs, apps/autoresearch-cli/src/template.rs</files>
  <action>Add Clap CLI named `autoresearch` with global repository path and JSON output. `init` writes missing commented `autoresearch.toml` and `program.md` using create-new semantics and never overwrites. `doctor` validates Git worktree, manifest, nonblank program, ignored run-state path, and availability of agent/evaluator executables without executing them. Return stable non-zero environment error when any required check fails.</action>
  <verify>cargo test -p autoresearch-cli init doctor</verify>
  <done>Init is idempotent and preserves bytes of existing files; doctor reports named pass/fail checks in text or JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze baseline run shell without fake evaluation</name>
  <files>apps/autoresearch-cli/src/lib.rs, apps/autoresearch-cli/src/baseline.rs, apps/autoresearch-cli/tests/cli.rs, README.md</files>
  <action>Add `baseline` that requires clean tracked `autoresearch.toml`, `program.md`, and optional `docs/BET.md`; resolves exact HEAD commit; captures canonical frozen identity; creates unique `.autoresearch/runs/<run-id>/`; copies frozen inputs; writes identity JSON; and writes sequence-zero `run_started` JSONL record. Report next state as `capture_baseline` and state plainly that evaluator evidence is pending. Add isolated temporary-Git integration test and concise Rust quick start. Do not run agent, evaluator, network, deployment, or external actions.</action>
  <verify>cargo test -p autoresearch-cli --all-features</verify>
  <done>Clean fixture creates replayable run-start journal and immutable copies; dirty/untracked control input refuses baseline; docs never claim baseline score exists.</done>
</task>

</tasks>

<verification>
- [ ] `cargo fmt --all --check`
- [ ] `cargo sort --workspace --check`
- [ ] `cargo clippy --workspace --all-targets --all-features --offline -- -D warnings`
- [ ] `cargo test --workspace --all-features --offline`
- [ ] `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --offline`
- [ ] CLI help lists only implemented foundation commands.
</verification>

<success_criteria>
- Init cannot overwrite repository contracts.
- Doctor remains read-only and does not invoke configured commands.
- Baseline refuses dirty frozen inputs and records exact commit/input identity.
- Baseline makes pending evaluator state explicit.
- Phase 1 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` with Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, Verification, and Next Step sections.
</output>
