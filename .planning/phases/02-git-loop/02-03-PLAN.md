---
phase: 02-git-loop
plan: 03
type: execute
domain: rust
---

<objective>
Contain candidate mutations before creating exactly one comparable Git commit.

Purpose: Prevent mutation agents from committing protected, out-of-scope, symlinked, ignored, or nested-repository state while keeping caller checkout untouched.
Output: Core-owned repository path and mutation-boundary contract plus a lock-bound Git candidate committer with adversarial integration proof.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within task scope, use direct Git argument arrays rather than shell strings, and create commits with plumbing commands that bypass repository hooks. Run each `<verify>` command and write SUMMARY.md after all gates pass. Leave candidate worktrees in place; Plan 02-04 owns journal-driven cleanup.
</execution_context>

<context>
Read these project files before editing:
- .planning/BRIEF.md
- .planning/ROADMAP.md
- .planning/phases/02-git-loop/02-02-SUMMARY.md
- docs/plans/2026-09-11-autoresearch-rust-platform-design.md
- crates/autoresearch-config/src/manifest.rs
- crates/autoresearch-core/src/workspace.rs
- crates/autoresearch-git/src/workspace.rs
- crates/autoresearch-git/src/repository.rs
- crates/autoresearch-git/src/error.rs
- crates/autoresearch-git/tests/workspace.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define inward-owned mutation boundaries and candidate commit port</name>
  <files>crates/autoresearch-core/src/containment.rs, crates/autoresearch-core/src/lib.rs, crates/autoresearch-core/tests/containment.rs, crates/autoresearch-config/src/manifest.rs, crates/autoresearch-config/src/lib.rs</files>
  <action>Move the validated repository-relative path value into core and preserve `autoresearch_config::RepoPath` and `Scope` as stable re-exports. Add a normalized `MutationBoundary` that requires at least one mutable root, checks protected roots before mutable roots, and returns typed violations for protected or outside-scope paths. Add `CandidateCommit`, which requires a non-empty sorted unique changed-path set, and a `CandidateCommitter` port accepting a run, candidate, and frozen boundary. Keep all filesystem and Git I/O out of core. Do not duplicate path semantics between config and core because drift could let manifest validation disagree with commit enforcement.</action>
  <verify>CARGO_BUILD_RUSTC_WRAPPER= cargo test -p autoresearch-core -p autoresearch-config --all-features --offline</verify>
  <done>Config retains its public path/scope API, unsafe paths and mutable/protected overlap still fail during manifest parsing, and pure tests prove component-aware allow/deny decisions plus non-empty canonical commit evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Create contained candidate commits without hooks or caller mutation</name>
  <files>crates/autoresearch-git/src/workspace.rs, crates/autoresearch-git/src/workspace/commit.rs, crates/autoresearch-git/src/workspace/identity.rs, crates/autoresearch-git/src/error.rs, crates/autoresearch-git/tests/containment.rs</files>
  <action>Implement the core candidate-commit port on `LockedGitRepository`. Revalidate lock-bound run identity, deterministic candidate path, detached HEAD, exact parent commit, candidate Git common-directory identity, and changed paths across staged, unstaged, untracked, and ignored state. Reject no-op candidates, protected/outside paths, non-UTF-8 names, changed symlinks or symlink ancestors, and nested `.git` repositories. After validation, stage the complete visible worktree, validate the staged diff again, create one fixed-message commit using `write-tree`, `commit-tree`, and compare-and-swap detached `HEAD` update, then prove the resulting direct-child diff contains exactly the validated paths and leaves the candidate clean. Never invoke `git commit` or hooks. Integration fixtures must prove allowed create/edit/delete succeeds; every hostile class fails before HEAD moves; caller branch, HEAD, index, and status never change.</action>
  <verify>CARGO_BUILD_RUSTC_WRAPPER= cargo test -p autoresearch-git --test containment --offline</verify>
  <done>Only declared mutable regular-file changes become one direct-child candidate commit, containment failures are typed and leave candidate HEAD unchanged, and caller checkout remains byte-for-byte untouched.</done>
</task>

</tasks>

<verification>
- [ ] `CARGO_BUILD_RUSTC_WRAPPER= cargo fmt --all --check`
- [ ] `cargo sort --workspace --check`
- [ ] `CARGO_BUILD_RUSTC_WRAPPER= cargo clippy --workspace --all-targets --all-features --offline -- -D warnings`
- [ ] `CARGO_BUILD_RUSTC_WRAPPER= cargo test --workspace --all-features --offline`
- [ ] `CARGO_BUILD_RUSTC_WRAPPER= RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --offline`
- [ ] Security grep confirms no shell command construction, `git commit`, hook execution, or unchecked destructive path operation in candidate commit adapter.
- [ ] Core dependency scan confirms no process, filesystem I/O, Git, config, or infrastructure imports.
</verification>

<success_criteria>
- Frozen mutable and protected roots use one domain-owned path model.
- Candidate commit rejects protected, outside, ignored, symlinked, nested-repository, foreign, attached, stale, and unchanged states.
- Successful mutation becomes exactly one clean direct-child commit with canonical changed-path evidence.
- Commit creation bypasses hooks and shell evaluation.
- Caller checkout remains untouched.
- Candidate worktree remains retained for evaluator and Plan 02-04 recovery.
</success_criteria>

<output>
After completion, create `.planning/phases/02-git-loop/02-03-SUMMARY.md` with Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, Verification, Security Review, and Next Step sections.
</output>
