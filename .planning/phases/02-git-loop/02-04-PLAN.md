---
phase: 02-git-loop
plan: 04
type: execute
domain: rust
---

<objective>
Reconcile journal-declared candidate state with Git and remove only proven run-owned worktrees.

Purpose: Make candidate preparation and keep/discard finalization idempotently resumable after crashes without guessing about ambiguous repository state or risking unrelated paths.
Output: Core recovery contract plus lock-bound Git reconciliation, exact worktree cleanup, and adversarial crash-boundary integration proof.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within task scope, use literal Git argument arrays rather than shell strings, and fail closed whenever journal, retained ref, linked-worktree registration, filesystem identity, cleanliness, or commit topology disagree. Run each `<verify>` command and write SUMMARY.md after all gates pass.
</execution_context>

<context>
Read these project files before editing:
- .planning/BRIEF.md
- .planning/ROADMAP.md
- .planning/phases/02-git-loop/02-03-SUMMARY.md
- docs/plans/2026-09-11-autoresearch-rust-platform-design.md
- crates/autoresearch-core/src/journal.rs
- crates/autoresearch-core/src/workspace.rs
- crates/autoresearch-git/src/workspace.rs
- crates/autoresearch-git/src/workspace/identity.rs
- crates/autoresearch-git/src/error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define journal-to-candidate recovery contract</name>
  <files>crates/autoresearch-core/src/journal.rs, crates/autoresearch-core/src/recovery.rs, crates/autoresearch-core/src/lib.rs, crates/autoresearch-core/tests/recovery.rs</files>
  <action>Bind every recorded candidate decision to the exact evaluated candidate commit and require matching keep finalization. Add pure recovery outcomes that distinguish a newly prepared or already committed candidate ready for evaluation from a completed keep/discard finalization. Add an inward-owned `CandidateRecoveryManager` port consuming validated `RunView` state. Preserve complete run, candidate, commit, and finalization evidence in returned values; return no candidate action for baseline, next-candidate, and stopped states. Keep Git, process, and filesystem I/O outside core.</action>
  <verify>CARGO_BUILD_RUSTC_WRAPPER= cargo test -p autoresearch-core --test recovery --offline</verify>
  <done>Application code can request one typed, journal-derived recovery operation and can tell whether mutation is still pending, evaluator-only work is ready, or finalization side effects have completed.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile Git effects and clean exact owned worktrees</name>
  <files>crates/autoresearch-git/src/workspace.rs, crates/autoresearch-git/src/workspace/recovery.rs, crates/autoresearch-git/src/error.rs, crates/autoresearch-git/tests/recovery.rs</files>
  <action>Implement the recovery port on `LockedGitRepository`. Rebuild expected run and deterministic candidate identity from replayed journal values; reject invalid IDs, foreign base state, worktree-ID disagreement, dirty or nonlinear candidates, unexpected ref movement, partial registration, symlink/path mismatch, and foreign lock reasons. For `EvaluateCandidate`, create a missing candidate or resume a proven clean detached parent/direct-child worktree. For `FinalizeCandidate`, idempotently apply or recognize the exact keep/discard decision, then unlock and invoke `git worktree remove` only after proving the exact registered candidate path belongs to this repository and run. Never recursively delete filesystem paths, prune unknown worktrees, use force on dirty state, or alter caller checkout. Cover crashes before/after prepare, ref advance, unlock, removal, and finalization plus hostile mismatches.</action>
  <verify>CARGO_BUILD_RUSTC_WRAPPER= cargo test -p autoresearch-git --test recovery --offline</verify>
  <done>Repeated recovery converges for safe preparation and both decisions; clean owned candidate worktrees disappear; ambiguous or foreign state remains untouched with typed errors; caller branch, HEAD, index, and status never change.</done>
</task>

</tasks>

<verification>
- [ ] `CARGO_BUILD_RUSTC_WRAPPER= cargo fmt --all --check`
- [ ] `cargo sort --workspace --check`
- [ ] `CARGO_BUILD_RUSTC_WRAPPER= cargo clippy --workspace --all-targets --all-features --offline -- -D warnings`
- [ ] `CARGO_BUILD_RUSTC_WRAPPER= cargo test --workspace --all-features --offline`
- [ ] `CARGO_BUILD_RUSTC_WRAPPER= RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --offline`
- [ ] Security scan confirms cleanup uses no shell construction, recursive filesystem deletion, broad path, glob, worktree prune, or unvalidated force removal.
- [ ] Core dependency scan confirms recovery contract imports no Git, process, filesystem-I/O, config, or infrastructure code.
</verification>

<success_criteria>
- Journal replay is sole source of requested candidate recovery transition.
- Preparation and keep/discard finalization are idempotent across supported crash boundaries.
- Git recovery never guesses when durable journal and observable Git state conflict.
- Cleanup removes only exact, clean, registered, run-owned candidate worktree.
- Caller checkout remains untouched.
- Phase 2 is complete and evaluator work can build on recoverable candidate state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-git-loop/02-04-SUMMARY.md` with Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, Verification, Security Review, and Next Step sections. Mark Phase 2 complete in `.planning/ROADMAP.md`.
</output>
