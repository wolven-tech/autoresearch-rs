---
phase: 01-foundation
plan: 02
type: execute
domain: rust
---

<objective>
Define and freeze trustworthy repository experiment configuration.

Purpose: Reject unsafe or ambiguous manifests before mutation and produce a stable identity that detects gate movement.
Output: Config crate with TOML schema, normalized repository paths, cross-field validation, canonical hashing, and tests.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within task scope, run each `<verify>` command, and write SUMMARY.md after all gates pass.
</execution_context>

<context>
Read these project files before editing:
- .planning/BRIEF.md
- .planning/ROADMAP.md
- .planning/phases/01-foundation/01-01-SUMMARY.md
- docs/plans/2026-09-11-autoresearch-rust-platform-design.md
- Cargo.toml
- crates/autoresearch-core/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse and validate versioned manifests</name>
  <files>Cargo.toml, crates/autoresearch-config/Cargo.toml, crates/autoresearch-config/src/lib.rs, crates/autoresearch-config/src/manifest.rs</files>
  <action>Add schema-version-1 TOML types for experiment budget/objective, mutable and protected paths, agent command, evaluators, declared outputs, and external authority ceilings. Parse into raw serde types, then validate into a separate public wrapper. Reject blank identifiers, duplicate evaluator/measurement names, absent or mismatched primary objective, extra objective metrics, unsafe paths, mutable paths inside protected paths, empty commands, zero timeouts, and zero run budgets. Automatically protect `autoresearch.toml` and `program.md`. Use argument arrays rather than shell strings.</action>
  <verify>cargo test -p autoresearch-config manifest</verify>
  <done>Valid example parses; every fail-closed branch has a focused unit test and callers cannot obtain a validated manifest without checks.</done>
</task>

<task type="auto">
  <name>Task 2: Compute canonical frozen input identity</name>
  <files>crates/autoresearch-config/src/identity.rs, crates/autoresearch-config/src/lib.rs</files>
  <action>Hash normalized validated manifest, program bytes, named frozen fixtures, and optional original product-gate bytes using SHA-256 with domain tags and length prefixes. Sort unordered path and fixture collections while preserving command-argument and evaluator execution order. Return both per-input digests and aggregate run identity so later diagnostics identify what moved. Avoid hashing filesystem metadata or absolute checkout paths because identities must survive worktree relocation.</action>
  <verify>cargo test -p autoresearch-config identity</verify>
  <done>Equivalent TOML key/path ordering yields same identity; changed program, gate, fixture, evaluator, or threshold changes identity; ambiguous concatenation cannot collide structurally.</done>
</task>

</tasks>

<verification>
- [ ] `cargo fmt --all --check`
- [ ] `cargo sort --workspace --check`
- [ ] `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- [ ] `cargo test --workspace --all-features`
- [ ] `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps`
</verification>

<success_criteria>
- Invalid manifests fail before runner side effects.
- Protected control files cannot become mutable.
- Primary objective has one immutable definition and direction.
- Frozen identity is deterministic, relocation-independent, and sensitive to every gate input.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, Verification, and Next Step sections.
</output>
