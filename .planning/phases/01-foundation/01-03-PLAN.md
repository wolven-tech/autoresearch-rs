---
phase: 01-foundation
plan: 03
type: execute
domain: rust
---

<objective>
Model append-only run history and deterministic crash recovery without filesystem side effects.

Purpose: Make partial experiment state explicit before Git and process layers depend on it.
Output: Core journal events, strict replay state machine, recovery action, and transition tests.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within task scope, run each `<verify>` command, and write SUMMARY.md after all gates pass.
</execution_context>

<context>
Read these project files before editing:
- .planning/BRIEF.md
- .planning/ROADMAP.md
- .planning/phases/01-foundation/01-02-SUMMARY.md
- docs/plans/2026-09-11-autoresearch-rust-platform-design.md
- crates/autoresearch-core/src/lib.rs
- crates/autoresearch-core/src/decision.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define journal events and replay state</name>
  <files>crates/autoresearch-core/src/journal.rs, crates/autoresearch-core/src/lib.rs</files>
  <action>Add sequence-numbered serialisable journal entries for run start, baseline capture, candidate preparation, decision recording, candidate finalization, and stop. Replay into immutable run view with explicit active candidate and next recovery action. Enforce one run identity, consecutive sequence numbers, candidate index order, matching candidate events, one finalization, nonblank commit/identity values, and no events after stop. Record decision before finalization side effect so recovery never guesses whether candidate should advance.</action>
  <verify>cargo test -p autoresearch-core journal</verify>
  <done>Valid journals replay deterministically from every partial state; illegal transitions return typed errors.</done>
</task>

<task type="auto">
  <name>Task 2: Prove crash boundaries and fail-closed replay</name>
  <files>crates/autoresearch-core/src/journal.rs</files>
  <action>Add focused tests for empty journal, started run, captured baseline, prepared candidate, recorded keep/discard decision, finalized candidate, stopped run, sequence gaps, identity mismatch, out-of-order candidates, duplicate events, and post-stop mutation. Assert exact recovery action at each valid prefix.</action>
  <verify>cargo test -p autoresearch-core journal -- --nocapture</verify>
  <done>Every side-effect boundary has a tested replay result and malformed histories cannot produce executable recovery state.</done>
</task>

</tasks>

<verification>
- [ ] `cargo fmt --all --check`
- [ ] `cargo sort --workspace --check`
- [ ] `cargo clippy --workspace --all-targets --all-features --offline -- -D warnings`
- [ ] `cargo test --workspace --all-features --offline`
- [ ] `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --offline`
</verification>

<success_criteria>
- Journal replay is pure and deterministic.
- Illegal, ambiguous, or cross-run histories fail closed.
- Recovery action identifies one next operation or completed run.
- Decision is durable before candidate finalization side effect.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` with Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, Verification, and Next Step sections.
</output>
