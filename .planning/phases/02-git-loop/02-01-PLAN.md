---
phase: 02-git-loop
plan: 01
type: execute
domain: rust
---

<objective>
Establish trustworthy repository identity and single-owner run access before creating candidate worktrees.

Purpose: Prevent experiments from starting against dirty, ambiguous, bare, or concurrently owned repositories.
Output: Pure repository-domain values and port plus a Git CLI adapter and crash-releasing advisory run lock.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within task scope, use argument arrays rather than shell strings, run each `<verify>` command, and write SUMMARY.md after all gates pass.
</execution_context>

<context>
Read these project files before editing:
- .planning/BRIEF.md
- .planning/ROADMAP.md
- .planning/phases/01-foundation/01-04-SUMMARY.md
- docs/plans/2026-09-11-autoresearch-rust-platform-design.md
- crates/autoresearch-core/src/lib.rs
- apps/autoresearch-cli/src/baseline.rs
- Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model and inspect clean Git repositories</name>
  <files>Cargo.toml, crates/autoresearch-core/src/lib.rs, crates/autoresearch-core/src/repository.rs, crates/autoresearch-git/Cargo.toml, crates/autoresearch-git/src/lib.rs, crates/autoresearch-git/src/repository.rs, crates/autoresearch-git/tests/repository.rs</files>
  <action>Add core-owned commit and repository snapshot value types plus a read-only `RepositoryInspector` port. Add `autoresearch-git` infrastructure adapter that invokes Git directly without a shell; canonicalizes the target root; requires a non-bare worktree, exact resolvable base commit and HEAD, clean tracked and untracked state, and ignored `.autoresearch/` run state. Reject blank refs and return typed diagnostics with bounded command stderr. Tests must use temporary local Git fixtures and prove inspection leaves caller checkout unchanged.</action>
  <verify>cargo test -p autoresearch-core -p autoresearch-git --all-features</verify>
  <done>Clean repository produces canonical root and exact commit IDs; dirty, bare, invalid-ref, and tracked-run-state fixtures fail with distinct typed errors.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce exclusive repository ownership</name>
  <files>crates/autoresearch-git/src/lib.rs, crates/autoresearch-git/src/lock.rs, crates/autoresearch-git/tests/lock.rs</files>
  <action>Add advisory lock at `.autoresearch/run.lock` acquired only from a validated snapshot. Write bounded JSON owner metadata after lock acquisition. A second owner must fail with a typed lock-held error; dropping or crashing releases kernel ownership without deleting the evidence file. Reject blank run IDs. Tests must prove reacquisition after guard drop, independent locks across repositories, readable owner metadata, and clean Git status throughout.</action>
  <verify>cargo test -p autoresearch-git --all-features</verify>
  <done>Only one guard can own a repository at a time, stale process ownership releases automatically, and lock operations never modify tracked product files.</done>
</task>

</tasks>

<verification>
- [ ] `cargo fmt --all --check`
- [ ] `cargo sort --workspace --check`
- [ ] `cargo clippy --workspace --all-targets --all-features --offline -- -D warnings`
- [ ] `cargo test --workspace --all-features --offline`
- [ ] `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --offline`
- [ ] Core has no dependency on Git or filesystem adapter crates.
</verification>

<success_criteria>
- Repository snapshot uses canonical absolute root and exact commit identities.
- Dirty or structurally unsafe repositories fail before mutation.
- Git adapter performs no shell evaluation and no checkout mutation.
- Advisory run lock guarantees one active owner and releases on process exit.
- Phase 2 next slice can create worktrees behind the validated port.
</success_criteria>

<output>
After completion, create `.planning/phases/02-git-loop/02-01-SUMMARY.md` with Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, Verification, and Next Step sections.
</output>
