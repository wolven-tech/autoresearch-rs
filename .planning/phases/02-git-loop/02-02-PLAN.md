---
phase: 02-git-loop
plan: 02
type: execute
domain: rust
---

<objective>
Create retained run branches and isolated candidate worktrees without moving or editing caller checkout.

Purpose: Give every mutation a recoverable Git boundary while current-best state advances only through explicit compare-and-swap retention.
Output: Core workspace lifecycle port plus lock-bound Git adapter with run-branch, candidate preparation, and retention integration tests.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within task scope, use direct process arguments rather than shell strings, run each `<verify>` command, and write SUMMARY.md after all gates pass. Do not delete worktrees or enforce mutable-path policy in this slice; later plans own containment and cleanup.
</execution_context>

<context>
Read these project files before editing:
- .planning/BRIEF.md
- .planning/ROADMAP.md
- .planning/phases/02-git-loop/02-01-SUMMARY.md
- docs/plans/2026-09-11-autoresearch-rust-platform-design.md
- crates/autoresearch-core/src/repository.rs
- crates/autoresearch-core/src/journal.rs
- crates/autoresearch-git/src/repository.rs
- crates/autoresearch-git/src/lock.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define workspace lifecycle values and port</name>
  <files>crates/autoresearch-core/Cargo.toml, crates/autoresearch-core/src/lib.rs, crates/autoresearch-core/src/workspace.rs, crates/autoresearch-core/tests/workspace.rs</files>
  <action>Add validated `RunId`, retained `RunWorkspace`, and isolated `CandidateWorkspace` values plus a `CandidateWorkspaceManager` port. Run IDs permit only bounded safe ASCII. Candidate indices are one-based; worktree IDs are deterministic; paths must be absolute and lexically normalized. Run branch ref is derived as `refs/heads/autoresearch/&lt;run-id&gt;`. Port exposes open/resume run, prepare candidate, and retain candidate operations while keeping all Git and filesystem I/O outside core.</action>
  <verify>cargo test -p autoresearch-core --all-features --offline</verify>
  <done>Invalid identifiers, zero candidate indices, relative/unnormalized paths, and mismatched run identities cannot form domain values; infrastructure can implement lifecycle behind one inward-owned trait.</done>
</task>

<task type="auto">
  <name>Task 2: Create or resume lock-bound run branch</name>
  <files>crates/autoresearch-git/src/lib.rs, crates/autoresearch-git/src/error.rs, crates/autoresearch-git/src/lock.rs, crates/autoresearch-git/src/repository.rs, crates/autoresearch-git/src/workspace.rs, crates/autoresearch-git/tests/workspace.rs</files>
  <action>Extract shared typed Git failures without changing public error API. Add `LockedGitRepository` constructed only from matching validated snapshot and live lock guard. `open_run` creates `refs/heads/autoresearch/&lt;run-id&gt;` at frozen base using atomic `git update-ref`, or resumes an existing descendant branch. Reject unrelated/diverged refs, mismatched lock ownership, and stale branch state. Never checkout branch in caller worktree.</action>
  <verify>cargo test -p autoresearch-git --test workspace open_run --offline</verify>
  <done>Run branch creation is idempotent, branch starts at exact base, divergence fails closed, and caller HEAD/status remain byte-for-byte unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Prepare detached candidate and retain by compare-and-swap</name>
  <files>crates/autoresearch-git/src/workspace.rs, crates/autoresearch-git/tests/workspace.rs</files>
  <action>Create candidate at `.autoresearch/worktrees/&lt;run-id&gt;/candidate-&lt;index&gt;` using detached `git worktree add`, then Git-lock it against pruning. Refuse existing or symlinked path components. `retain_candidate` must verify adapter/run/candidate identity, candidate clean state, and exactly one parent equal to current run head before atomically advancing retained ref with expected-old `git update-ref`. Return updated run value. Leave worktree intact for journal-driven cleanup in Plan 02-04; do not stage or commit candidate changes before Plan 02-03 containment validation exists.</action>
  <verify>cargo test -p autoresearch-git --test workspace --offline</verify>
  <done>Candidate is detached at current-best commit, caller checkout never moves or becomes dirty, retention advances only expected run ref, and stale/foreign/dirty/nonlinear candidates fail closed.</done>
</task>

</tasks>

<verification>
- [ ] `cargo fmt --all --check`
- [ ] `cargo sort --workspace --check`
- [ ] `cargo clippy --workspace --all-targets --all-features --offline -- -D warnings`
- [ ] `cargo test --workspace --all-features --offline`
- [ ] `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --offline`
- [ ] Caller branch, HEAD commit, index, and porcelain status remain unchanged in lifecycle integration tests.
- [ ] Core has no dependency on Git or infrastructure crates.
</verification>

<success_criteria>
- One retained run ref represents current-best commit.
- Candidate worktree starts detached from exact retained head.
- All writes require matching live repository lock.
- Run ref advances atomically only for clean direct-child candidate commit.
- Caller checkout remains untouched throughout branch/worktree lifecycle.
- Phase 2 containment slice can validate candidate diff before commit/retention.
</success_criteria>

<output>
After completion, create `.planning/phases/02-git-loop/02-02-SUMMARY.md` with Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, Verification, and Next Step sections.
</output>
